End every python script with a dashed line like below.

---------------------------------------------------------------------------------------------------

Make sure that this line is at the very end, and on a separate line, with no trailing spaces.