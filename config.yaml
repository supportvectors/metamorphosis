
# The list of models used
llm_models:
  mini: gpt-5-mini
  reasoning: gpt-5
  non-reasoning: gpt-4.1

# ---------------------------------------------------------
# The list of models used for text modifiers
# ---------------------------------------------------------
text_modifier_models:
  copy_editor: 
    model: gpt-5-mini
    temperature: 0.1
    max_tokens: 4000
    top_p: 1
    frequency_penalty: 0
    presence_penalty: 0
    stop_sequences: []
    timeout: 30

  # ---------------------------------------------------------
  summarizer:
    model: gpt-5  # we need reasoning here
    temperature: 0.0
    max_tokens: 4000
    timeout: 300


